image: node:lts

pages:
  before_script:
    # Clean public folder
    - find public -mindepth 1 -maxdepth 1 -type d | xargs rm -rf
    - find public -type f -name "*.html" | xargs rm -rf
    # Install packages
    - npm install
  script:
    # Build application and move content to public folder
    - npm run publish
    - rm -rf public/*
    - mv out/* public
  after_script:
    # Cleanup
    - rm -rf out
  artifacts:
    paths:
      - public
  only:
    - master

cache:
  paths:
    - node_modules/

before_script:
  - npm install -g firebase-tools

deploy:
  stage: deploy
  before_script:
    # Clean public folder
    - find public -mindepth 1 -maxdepth 1 -type d | xargs rm -rf
    - find public -type f -name "*.html" | xargs rm -rf
    # Install packages and Firebase CLI
    - npm install
    - npm install -g firebase-tools
  script:
    # Build application and move content to public folder
    - npm run publish
    - rm -rf public/*
    - mv out/* public
    - firebase deploy --only hosting --project $FIREBASE_PROJECT_ID --token $FIREBASE_TOKEN
  only:
    - develop


variables:
  FIREBASE_PROJECT_ID: "ai-website-34d0b"
  FIREBASE_TOKEN: "1//0gyz8hxpXRdQkCgYIARAAGBASNwF-L9IrEQrWaQJFSZ6JDak_v9u9uJ4ue9MQp_fz7edxMhByTGcS_qBSwYJwrZGRPUN6lz7ktbs"

